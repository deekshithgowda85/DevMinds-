# Include native binaries for all target platforms in package-lock.json.
# This prevents "Cannot find native binding" errors when the lock file is
# generated on Windows but npm ci runs on Linux (CI / Vercel / AWS).
supported-architectures-os=linux,darwin,win32
supported-architectures-cpu=x64,arm64
